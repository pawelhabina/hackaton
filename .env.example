# Required for AI-generated chat replies.
GEMINI_API_KEY=your_gemini_api_key

# Optional. Default model used by the chat backend.
GEMINI_MODEL=gemini-2.5-flash

# Optional. Local backend port for /api/chat.
CHAT_PORT=8787
